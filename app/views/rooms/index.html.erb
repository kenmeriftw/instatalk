<p id="notice"><%= notice %></p>

<h1>Rooms</h1>

<ul class="mt-4 list-unstyled">
  <%= render @rooms %>
</ul>

<div class="mt-2">
  <%= render 'rooms/form', room: @room %>
</div>

<br/>

<div class="mt-2">
  <h1>Online now:</h1>
    <ul class="userlist" id="online"></ul>
</div>


